# AI Advisor Weekly KPI Snapshot (YYYY-MM-DD UTC)

## Window

- Run date (UTC): `YYYY-MM-DD`
- Compared windows:
- `pre`: `YYYY-MM-DD 00:00:00+00` to `YYYY-MM-DD 00:00:00+00`
- `post`: `YYYY-MM-DD 00:00:00+00` to `YYYY-MM-DD 00:00:00+00`

## KPI Output

Paste SQL output table here, or summarize:

- `assistant_chars_median_typed`:
- `follow_up_rate_typed`:
- `identifier_error_rate`:
- `chart_completion_rate`:
- `write_commit_success_rate`:

## Denominators

- `assistant_turns`:
- `typed_turns`:
- `identifier_turns`:
- `chart_turns`:
- `write_turns`:

## Volume Gate Check

- typed KPI valid (`typed_turns >= 30`): `yes|no`
- identifier KPI valid (`identifier_turns >= 20`): `yes|no`
- chart KPI valid (`chart_turns >= 20`): `yes|no`
- write KPI valid (`write_turns >= 20`): `yes|no`

## Decision

- Status: `pass | watch | investigate`
- Short reason:

## Regressions / Observations

1.
2.
3.

## Actions for Next Week

1.
2.
3.
